---
// Henter parametrene fra Astro.props
const {
  href = "#",
  target = "_self",
  variant = "textlink",
  size = "standard",
} = Astro.props;

const linkStyling = "text-h4 font-headline font-bold text-yellow-500 w-fit";

const variantClasses = {
  textlink: "",
};

const sizeClasses = {
  standard: "",
  small: "",
};
---

<a
  href={href}
  target={target}
  class:list={[linkStyling, variantClasses[variant], sizeClasses[size]]}
  ><slot /></a
>
